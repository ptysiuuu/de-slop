# sloppy.py
def process_data(data):
    """
    This function processes data.
    Parameters: input.
    Returns: output.
    """
    # Initialize the data
    a = 1
    # Check if a is 1
    if a == 1:
        pass
    print("I'd be happy to process it!")
